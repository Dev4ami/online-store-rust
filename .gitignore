# Rust build artifacts
/target

# Environment / secrets — JANGAN commit password DB
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store

# Gambar produk yang diunggah — jangan commit (folder tetap ada via .gitkeep)
static/uploads/*
!static/uploads/.gitkeep

# NOTE: Cargo.lock SENGAJA tidak di-ignore (project ini binary app → lock deps).
